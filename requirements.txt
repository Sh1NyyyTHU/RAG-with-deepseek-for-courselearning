# Core dependencies (versions tested and working)
streamlit>=1.32.0
chromadb>=0.5.0
pymupdf>=1.24.0
openai>=1.30.0
FlagEmbedding>=1.4.0
sentence-transformers>=5.0.0
transformers>=5.0.0
python-dotenv>=1.0.0
numpy>=1.26.0
pytest>=8.0.0
torch>=2.0.0

# Note: FlagEmbedding requires transformers >= 5.0.0 for XLMRobertaModel dtype support

# OCR (optional — MVP runs without these)
# paddlepaddle>=3.0.0
# paddleocr>=2.9.0
